{
  "installCommand": "corepack enable && corepack prepare pnpm@9.15.4 --activate && pnpm config set registry https://registry.npmjs.org/ && pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm run build"
}
